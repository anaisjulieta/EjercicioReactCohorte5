export default function 
   Personaje({pers})  {
    
        return <pre>{JSON.stringify(pers)}</pre>
    }
/*     Personaje({pers2})  {
    
        return <pre>{JSON.stringify(pers2)}</pre>
    } */
   
    /* Personaje({pers,pers2}) {
       return (<pre>{JSON.stringify(pers)}</pre>,
       <pre>{JSON.stringify(pers2)}</pre>) */ 
